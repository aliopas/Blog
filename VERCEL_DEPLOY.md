# دليل رفع المشروع على Vercel 🚀

## ملخص سريع

المشروع جاهز تقريبًا للنشر على Vercel. هذا الدليل يشرح الخطوات المطلوبة.

## ✅ ما تم إنجازه

1. **ملف `vercel.json`**: موجود ومُعد بشكل صحيح
2. **المتغيرات البيئية**: موثقة في `.env.example`
3. **`.gitignore`**: مُحدّث لحماية `.env`
4. **توافق Next.js 15**: تم إصلاح مشكلة `params` في الصفحات الديناميكية
5. **ملف `DEPLOYMENT.md`**: دليل شامل موجود

## 📋 الخطوات المطلوبة

### 1. دفع الكود إلى GitHub

```bash
git add .
git commit -m "Prepare for Vercel deployment"
git push origin main
```

### 2. ربط المشروع بـ Vercel

#### الطريقة الأولى: عبر Vercel Dashboard (موصى بها)

1. اذهب إلى [vercel.com/new](https://vercel.com/new)
2. اختر الريبو الخاص بك من GitHub
3. Vercel سيكتشف تلقائيًا أنه مشروع Next.js

#### الطريقة الثانية: عبر Vercel CLI

```bash
# تثبيت Vercel CLI
npm i -g vercel

# تسجيل الدخول
vercel login

# نشر المشروع
vercel

# نشر للإنتاج
vercel --prod
```

### 3. إعداد المتغيرات البيئية

يجب إضافة هذه المتغيرات في **Vercel Dashboard** → **Settings** → **Environment Variables**:

#### البيانات الإدارية
```
ADMIN_EMAIL=admin@yourdomain.com
ADMIN_PASSWORD=your_secure_password
```

#### قاعدة البيانات (Aiven Postgres)
```
DB_HOST=your-db-host.aivencloud.com
DB_PORT=28899
DB_NAME=defaultdb
DB_USER=your-db-user
DB_PASSWORD=your-db-password
DB_SSL=true
```

#### مفاتيح Gemini API
```
GOOGLE_GENAI_API_KEY_1=your-api-key-1
GOOGLE_GENAI_API_KEY_2=your-api-key-2
```

#### مفتاح Cron
```
CRON_SECRET=your-random-secret-token
```

**ملاحظة**: اضبط كل متغير ليكون متاحًا في: **Production, Preview, Development**

### 4. بناء المشروع (اختباري - اختياري)

قبل النشر، يمكنك اختبار البناء محليًا:

```bash
npm run build
```

⚠️ **ملاحظة**: إذا فشل البناء محليًا بسبب متغيرات البيئة، لا تقلق. Vercel سيبني المشروع بنجاح بعد إضافة المتغيرات البيئية.

### 5. التحقق بعد النشر

بمجرد نشر المشروع:

1. ✅ تفقد الصفحة الرئيسية
2. ✅ جرب تسجيل الدخول على `/admin`
3. ✅ تأكد من عمل Dashboard على `/admin/dashboard`
4. ✅ اختبر إنشاء مقال جديد

## 🔧 إعدادات إضافية (اختياري)

### إعداد Domain مخصص

1. Dashboard → **Settings** → **Domains**
2. أضف الدومين الخاص بك
3. اتبع تعليمات DNS

### إعداد Cron Jobs (للمحتوى التلقائي)

في `vercel.json`:

```json
{
  "crons": [
    {
      "path": "/api/cron/generate",
      "schedule": "0 0 * * *"
    }
  ]
}
```

## 🐛 حل المشاكل

### مشكلة البناء (Build Error)

- تأكد من إضافة جميع المتغيرات البيئية
- تحقق من Logs في Vercel Dashboard
- راجع أخطاء TypeScript محليًا: `npm run typecheck`

### مشكلة قاعدة البيانات

- تأكد من أن قاعدة البيانات تسمح بالاتصال من Vercel
- راجع الـ connection string

### مشكلة AI

- تحقق من صلاحية مفاتيح Google Gemini API
- تأكد من Quota المتاحة

## 📚 مراجع إضافية

- [Vercel Deployment Docs](https://vercel.com/docs/deployments/overview)
- [Next.js Deployment](https://nextjs.org/docs/app/building-your-application/deploying)
- ملف `DEPLOYMENT.md` للمزيد من التفاصيل

## 📝 ملاحظات مهمة

1. **لا ترفع ملف `.env`** إلى GitHub أبدًا
2. **استخدم كلمات مرور قوية** للـ Admin
3. **احتفظ بنسخة احتياطية** من مفاتيح API
4. **راقب الاستخدام** لمفاتيح Gemini API لتجنب تجاوز الحدود

---

**جاهز للنشر! 🎉**

إذا واجهت أي مشاكل، راجع الـ Logs في Vercel Dashboard أو ملف `DEPLOYMENT.md`.
