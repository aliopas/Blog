# 🚀 Quick Start Guide - Automated Content Generation

## ✅ What We Built

تم إنشاء نظام كامل لتوليد المحتوى التلقائي باستخدام الذكاء الاصطناعي:

### المميزات:
1. ✅ **توليد محتوى تلقائي** - 6 مرات يومياً (كل 4 ساعات)
2. ✅ **اكتشاف المواضيع الرائجة** - باستخدام AI
3. ✅ **فحص الجودة** - قبل الحفظ في قاعدة البيانات
4. ✅ **نظام تدوير API Keys** - لتجنب تجاوز الحصة
5. ✅ **لوحة تحكم Admin** - للتحكم اليدوي ومراقبة النظام

---

## 📋 الخطوات السريعة للتشغيل

### 1️⃣ إضافة API Keys إلى قاعدة البيانات

افتح ملف `scripts/insert-api-keys.sql` وعدّل القيم:

```sql
INSERT INTO blog_api_keys (key_name, key_value, usage_count, quota_exceeded, created_at)
VALUES 
  ('GEMINI_API_KEY_1', 'ضع_المفتاح_الأول_هنا', 0, false, NOW()),
  ('GEMINI_API_KEY_2', 'ضع_المفتاح_الثاني_هنا', 0, false, NOW()),
  ('GEMINI_API_KEY_3', 'ضع_المفتاح_الثالث_هنا', 0, false, NOW());
```

ثم نفذ الملف في قاعدة البيانات.

### 2️⃣ تحديث CRON_SECRET

في ملف `.env`:
```env
CRON_SECRET=اختر_كلمة_سر_قوية_هنا
```

### 3️⃣ اختبار النظام يدوياً

1. شغل السيرفر:
```bash
npm run dev
```

2. افتح لوحة التحكم:
```
http://localhost:3000/admin/dashboard/automation
```

3. اضغط على **"Generate Article Now"**

4. انتظر النتيجة (قد يستغرق 30-60 ثانية)

5. إذا نجح، ستجد المقال في صفحة **Posts** بحالة **Draft**

### 4️⃣ إعداد Cron Job (للتشغيل التلقائي)

#### الخيار الأول: Vercel Cron (موصى به)

أنشئ ملف `vercel.json` في المجلد الرئيسي:

```json
{
  "crons": [
    {
      "path": "/api/cron",
      "schedule": "0 */4 * * *"
    }
  ]
}
```

#### الخيار الثاني: خدمة Cron خارجية

استخدم موقع مثل [cron-job.org](https://cron-job.org):

- **URL**: `https://your-domain.com/api/cron`
- **Method**: GET
- **Header**: `Authorization: Bearer your-cron-secret`
- **Schedule**: `0 */4 * * *` (كل 4 ساعات)

---

## 📁 الملفات المهمة

### AI Flows:
- `src/ai/flows/automated-content-generation.ts` - المنطق الرئيسي
- `src/ai/flows/check-article-quality.ts` - فحص الجودة
- `src/ai/genkit.ts` - إدارة AI instances

### Backend:
- `src/lib/api-key-manager.ts` - إدارة API keys
- `src/lib/cron-scheduler.ts` - جدولة المهام
- `src/lib/data.ts` - عمليات قاعدة البيانات

### API Routes:
- `src/app/api/cron/route.ts` - نقطة Cron الرئيسية
- `src/app/api/automation/generate/route.ts` - التشغيل اليدوي
- `src/app/api/automation/status/route.ts` - حالة النظام

### Admin UI:
- `src/app/admin/dashboard/automation/page.tsx` - لوحة التحكم

---

## 🔍 كيف يعمل النظام؟

```
1. Cron Job يشتغل كل 4 ساعات
        ↓
2. AI يبحث عن مواضيع رائجة في مجالك
        ↓
3. يختار أفضل موضوع
        ↓
4. يولد مقال كامل (1500-2500 كلمة)
        ↓
5. يفحص جودة المقال
        ↓
6. يحفظ المقال كـ Draft في قاعدة البيانات
        ↓
7. أنت تراجع المقال وتنشره
```

---

## 🗓️ جدول التشغيل

| الوقت (UTC) | الوقت (مصر) | الحالة |
|------------|------------|--------|
| 00:00      | 02:00 AM   | ✅     |
| 04:00      | 06:00 AM   | ✅     |
| 08:00      | 10:00 AM   | ✅     |
| 12:00      | 02:00 PM   | ✅     |
| 16:00      | 06:00 PM   | ✅     |
| 20:00      | 10:00 PM   | ✅     |

**النتيجة**: 6 مقالات جديدة يومياً! 🎉

---

## 🐛 حل المشاكل

### المقالات لا تُولَّد؟

1. **تحقق من API Keys**:
```sql
SELECT key_name, usage_count, quota_exceeded 
FROM blog_api_keys;
```

2. **تحقق من Logs**:
```bash
# في terminal السيرفر
# ابحث عن رسائل مثل:
# ✅ Article generated
# ❌ Error in automated content generation
```

3. **جرب يدوياً**:
- افتح `/admin/dashboard/automation`
- اضغط "Generate Article Now"
- شوف الخطأ إن وجد

### API Quota Exceeded؟

- النظام يدور تلقائياً على المفاتيح الأخرى
- أضف مفاتيح إضافية في قاعدة البيانات
- المفاتيح تُعاد ضبطها يومياً في منتصف الليل

### جودة المقالات منخفضة؟

عدّل الـ prompts في:
- `src/ai/flows/automated-content-generation.ts`
- السطر 127: `prompt: ...`

---

## 📊 مراقبة النظام

### من لوحة التحكم:
```
/admin/dashboard/automation
```

### من API:
```bash
curl http://localhost:3000/api/automation/status
```

### من قاعدة البيانات:
```sql
-- آخر المقالات المولدة
SELECT title, status, created_at 
FROM blog_posts 
WHERE status = 'draft'
ORDER BY created_at DESC 
LIMIT 10;

-- إحصائيات API Keys
SELECT 
  key_name,
  usage_count,
  quota_exceeded,
  last_used_at
FROM blog_api_keys;
```

---

## 🎯 الخطوات التالية

1. ✅ اختبر النظام يدوياً
2. ✅ راجع أول مقال مولد
3. ✅ عدّل الـ prompts حسب احتياجك
4. ✅ فعّل Cron Job
5. ✅ راقب النتائج يومياً
6. ✅ انشر المقالات الجيدة

---

## 💡 نصائح

- **راجع المقالات قبل النشر** - AI ممتاز لكن المراجعة البشرية مهمة
- **عدّل العناوين** - اجعلها أكثر جاذبية
- **أضف صور** - استخدم `image_hint` لتوليد صور مناسبة
- **تحسين SEO** - راجع الـ keywords والـ meta descriptions
- **تنويع المواضيع** - غيّر الـ niche في `automated-content-generation.ts`

---

## 📚 مستندات إضافية

- [AUTOMATION.md](./AUTOMATION.md) - دليل شامل
- [insert-api-keys.sql](../scripts/insert-api-keys.sql) - سكريبت إضافة المفاتيح

---

## 🎉 تهانينا!

لديك الآن نظام كامل لتوليد المحتوى التلقائي! 🚀

**أسئلة؟** راجع الـ logs أو افحص الكود في الملفات المذكورة أعلاه.
