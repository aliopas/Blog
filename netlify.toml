[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# To schedule the AI automation to run every 4 hours (6 times/day):
# Use an external cron service (like cron-job.org or EasyCron) to hit:
# https://your-domain.com/api/cron/generate
# Schedule: 0 */4 * * * (every 4 hours)
